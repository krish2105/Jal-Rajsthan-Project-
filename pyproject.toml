[project]
name = "jal"
version = "0.1.0"
description = "JAL (जल) — Rajasthan Groundwater Risk & Recharge Prioritiser"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pandas>=2.2",
    "duckdb>=1.1",
    "pyarrow>=17",
    "pdfplumber>=0.11",
    "camelot-py[base]>=0.11",
    "rapidfuzz>=3.9",
    "geopandas>=1.0",
    "shapely>=2.0",
    "requests>=2.32",
    "pydantic>=2.8",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "pyyaml>=6.0",
    "pypdfium2>=5.13.0",
    "rasterio>=1.5.1",
    "rank-bm25>=0.2.2",
    "tensorflow>=2.21.0",
    "tf-keras>=2.21.0",
    "psycopg[binary]>=3.3.4",
    "sentry-sdk[fastapi]>=2.68.0",
    "torch>=2.13.0",
]

[project.optional-dependencies]
ml = [
    "lightgbm>=4.5",
    "scikit-learn>=1.5",
    "shap>=0.52.0",
    "pykrige>=1.7",
    "pulp>=3.3.2",
]
agents = [
    "openai>=3.1.0",
    "httpx>=0.28.1",
]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.3",
    "httpx>=0.28.1",
    "tabulate>=0.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jal"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
# third-party code kept verbatim so it stays diffable against upstream
extend-exclude = ["vendor"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
